services:
  pipeline:
    build: .
    image: video-ai-thesis
    volumes:
      - ./data:/app/data
      - ./models:/app/models
      - ./main.py:/app/main.py
      - ./config.py:/app/config.py
      - ./download_models.py:/app/download_models.py
      - ./pyproject.toml:/app/pyproject.toml
      - ./src:/app/src
    gpus: all
